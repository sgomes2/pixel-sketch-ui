import "./App.css";
import PixelGrid from "./components/PixelGrid/PixelGrid";

function App() {
  return (
    <div className="App">
      <PixelGrid gridSize={10} />
    </div>
  );
}

export default App;
